from render import render
from scratchpad import temp_build_brain

def main():
    render()
    # brain = temp_build_brain(num_inputs=1, num_hidden=2, num_outputs=1)

if __name__ == "__main__":
    main()
